<Window x:Class="AppClient.View.CarListView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:local="clr-namespace:AppClient.View"
        xmlns:components="clr-namespace:AppClient.Components"
        xmlns:viewModel="clr-namespace:AppClient.ViewModels;assembly=AppClient"
        mc:Ignorable="d"
        Title="Car List" Height="450" Width="800">

    <!--<Window.DataContext>
        <viewModel:CarListViewModel/>
    </Window.DataContext>

    <Window.Resources>
        <DataTemplate DataType="{x:Type components:CarListItemViewModel}">
            <components:CarListItem/>
        </DataTemplate>
    </Window.Resources>-->

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>


        <ListView Grid.Row="1" ItemsSource="{Binding Cars}" Background="{DynamicResource MaterialDesignPaper}">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <components:CarListItem DataContext="{Binding}"/>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Window>
