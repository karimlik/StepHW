<UserControl x:Class="AppClient.Components.CarListItem"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="120" d:DesignWidth="600">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="120"/>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <Grid Margin="8">
            <Grid.Background>
                <ImageBrush ImageSource="{Binding ImageUrl}" Stretch="UniformToFill" />
            </Grid.Background>
        </Grid>

        <StackPanel Grid.Column="1" Margin="16,8,8,8">
            <TextBlock Text="{Binding Make}" FontWeight="Bold" FontSize="20" Margin="0,0,0,4"/>
            <TextBlock Text="{Binding Model}" FontSize="18"  Margin="0,0,0,4"/>
            <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                <TextBlock Text="{Binding Year}" FontSize="16" Margin="0,0,8,0"/>
                <TextBlock Text="{Binding Mileage}" FontSize="16" Margin="0,0,8,0"/>
                <TextBlock Text="{Binding Price, StringFormat={}{0:C2}}" FontSize="16"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>
